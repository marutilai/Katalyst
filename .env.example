# Katalyst Environment Variables
# Copy this file to .env and fill in your values

# --- API Keys (Required based on provider) ---
# OpenAI (default provider)
OPENAI_API_KEY=

# Alternative providers (uncomment as needed)
# ANTHROPIC_API_KEY=
# GROQ_API_KEY=
# TOGETHER_API_KEY=

# --- Common Configuration Overrides ---
# Change these only if you need different values than defaults

# LLM provider: openai (default), anthropic, ollama, groq, together
# KATALYST_LLM_PROVIDER=openai

# Auto-approve file modifications (default: false)
# KATALYST_AUTO_APPROVE=true

# Custom model selection (defaults use provider's best models)
# KATALYST_REASONING_MODEL=gpt-4o
# KATALYST_EXECUTION_MODEL=gpt-4o-mini

# Increase limits for complex tasks
# KATALYST_RECURSION_LIMIT=500
# KATALYST_MAX_OUTER_CYCLES=10

# For Ollama users: custom API endpoint
# KATALYST_LLM_API_BASE=http://localhost:11434