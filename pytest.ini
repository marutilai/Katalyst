[pytest]
markers =
    unit: Marks tests as unit tests (fast, no external services).
    integration: Marks tests as integration tests (slower, may use filesystem).
    e2e: Marks tests as full end-to-end tests (very slow, expensive, uses LLMs).